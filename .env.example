# =============================================================================
# Template Backend - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the appropriate values for your environment.
#
# DO NOT commit the actual .env file to version control. It contains sensitive
# credentials and should be protected appropriately.
#
# =============================================================================
# ENVIRONMENT SELECTION
# =============================================================================
# Controls which Django settings module is loaded:
#   - development: Local development (default)
#   - staging: Pre-production testing environment
#   - production: Production deployment
#   - test: Running tests
#DJANGO_ENV=development

# =============================================================================
# DJANGO CORE SETTINGS
# =============================================================================

# SECRET_KEY: Django's secret cryptographic key.
# - Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# - REQUIRED: Yes
# - NOTE: Keep this value secret in production. Use a strong, random value.
SECRET_KEY=your-secret-key-here

# DEBUG: Enables debug mode.
# - Values: "True" or "False"
# - Development: Set to "True" for detailed error pages
# - Production: MUST be "False" for security
DEBUG="True"

# DJANGO_PORT: Port number for the development server.
# - Default: 8000
DJANGO_PORT=8000

# =============================================================================
# JWT AUTHENTICATION CONFIGURATION
# =============================================================================

# JWT_SIGNING_KEY: Key used to sign JWT tokens.
# - OPTIONAL: Defaults to SECRET_KEY if not provided
# - NOTE: In production, use a different key than SECRET_KEY for enhanced security
#JWT_SIGNING_KEY=your-jwt-signing-key-here

# JWT_ACCESS_TOKEN_LIFETIME_DAYS: Access token validity period.
# - Default: 10 days
# - Shorter values are more secure but require more frequent refreshes
JWT_ACCESS_TOKEN_LIFETIME_DAYS=10

# JWT_REFRESH_TOKEN_LIFETIME_DAYS: Refresh token validity period.
# - Default: 15 days
# - Should be longer than ACCESS_TOKEN_LIFETIME
JWT_REFRESH_TOKEN_LIFETIME_DAYS=15

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================

# DB_NAME: Name of the PostgreSQL database.
# - The database must exist before running migrations
# - Create with: createdb <DB_NAME>
DB_NAME=template_db

# DB_USER: PostgreSQL user with access to the database.
# - Ensure the user has necessary privileges
DB_USER=postgres

# DB_PASSWORD: Password for the database user.
# - Use a strong password in production
DB_PASSWORD=your-db-password-here

# DB_HOST: PostgreSQL server hostname.
# - Local development without Docker: 'localhost'
# - With Docker Compose: 'db' (service name)
# - Production: Your database server hostname or IP
#DB_HOST=localhost

# DB_PORT_NUMBER: PostgreSQL server port.
# - Default PostgreSQL port: 5432
DB_PORT_NUMBER=5432

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# Redis is used for:
#   - Caching
#   - Django Channels (WebSocket) layer
#   - Celery result backend
#   - Celery cache backend
# =============================================================================

# REDIS_HOST: Redis server hostname.
# - Local development without Docker: 'localhost'
# - With Docker Compose: 'redis' (service name)
# - Production: Your Redis server hostname
#REDIS_HOST=localhost

# REDIS_PORT_NUMBER: Redis server port.
# - Default Redis port: 6379
REDIS_PORT_NUMBER=6379

# REDIS_PASSWORD: Password for Redis authentication.
# - Required if your Redis server is password-protected
# - Leave empty if no password is set
REDIS_PASSWORD=your-redis-password-here

# =============================================================================
# RABBITMQ CONFIGURATION (Celery Message Broker)
# =============================================================================
# RabbitMQ is used as the message broker for Celery task queue.
# =============================================================================

# RABBITMQ_HOST: RabbitMQ server hostname.
# - Local development without Docker: 'localhost'
# - With Docker Compose: 'rabbitmq' (service name)
# - Production: Your RabbitMQ server hostname
#RABBITMQ_HOST=localhost

# RABBITMQ_PORT_NUMBER: RabbitMQ server port.
# - Default AMQP port: 5672
RABBITMQ_PORT_NUMBER=5672

# RABBITMQ_USER: RabbitMQ username.
# - Default: 'guest' (for local RabbitMQ)
# - Production: Create a dedicated user
RABBITMQ_USER=guest

# RABBITMQ_PASSWORD: Password for RabbitMQ authentication.
# - Default: 'guest' (for local RabbitMQ)
# - Production: Use a strong password
RABBITMQ_PASSWORD=your-rabbitmq-password-here

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Used for sending transactional emails (password resets, notifications, etc.)
# =============================================================================

# EMAIL_HOST: SMTP server hostname.
# - Examples: smtp.gmail.com, smtp.zoho.com, smtp.office365.com
# - Your email provider will provide this
EMAIL_HOST=smtp.your-provider.com

# EMAIL_PORT: SMTP server port.
# - Common values: 587 (TLS), 465 (SSL), 25 (unencrypted)
# - Use 587 for TLS (recommended)
EMAIL_PORT=587

# EMAIL_HOST_USER: SMTP authentication username.
# - Usually your full email address
EMAIL_HOST_USER=your-email@example.com

# EMAIL_HOST_PASSWORD: SMTP authentication password.
# - Use an app-specific password if required by your provider
EMAIL_HOST_PASSWORD=your-email-password-here

# EMAIL_USE_TLS: Enable TLS encryption for SMTP.
# - Values: "True" or "False"
# - Recommended: "True"
EMAIL_USE_TLS="True"

# =============================================================================
# STRIPE PAYMENT CONFIGURATION
# =============================================================================
# Used for subscription payments and billing.
# =============================================================================

# STRIPE_ENABLED: Enable/disable Stripe integration.
# - Values: "True" or "False"
# - Set to "False" during development without payment processing
STRIPE_ENABLED="False"

# STRIPE_SECRET_KEY: Stripe API secret key.
# - Get from: https://dashboard.stripe.com/apikeys
# - Test keys start with sk_test_
# - Live keys start with sk_live_
# - Required if STRIPE_ENABLED is "True"
#STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key-here

# STRIPE_PUBLISHABLE_KEY: Stripe publishable key (safe for frontend).
# - Get from: https://dashboard.stripe.com/apikeys
# - Test keys start with pk_test_
# - Live keys start with pk_live_
# - Required if STRIPE_ENABLED is "True"
#STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# =============================================================================
# URL CONFIGURATION
# =============================================================================

# FRONTEND_URL: Base URL of your frontend application.
# - Used for CORS configuration, email links, redirects
# - Include protocol (https:// or http://)
# - Example: https://template.com
FRONTEND_URL=https://your-frontend-domain.com

# API_URL: Base URL of this Django API backend.
# - Used for callbacks, webhooks, and internal references
# - Include protocol (https:// or http://)
# - Development: http://localhost:8000
# - Production: Your production API URL
API_URL=http://localhost:8000

# =============================================================================
# DOCKER CONFIGURATION
# =============================================================================
# DOCKER_ENV: Indicates if running inside Docker.
# - Values: "true" or "false"
# - Automatically set by docker-compose; usually don't set manually
# - Affects hostname resolution for database, Redis, RabbitMQ
#DOCKER_ENV=false

# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit .env to version control
#    Add .env to your .gitignore file
#
# 2. Use strong, unique passwords for all services in production
#
# 3. Rotate SECRET_KEY and JWT_SIGNING_KEY periodically
#
# 4. Use different keys for development, staging, and production
#
# 5. Restrict database and Redis access to specific IPs when possible
#
# 6. Use environment-specific Stripe test keys for development/staging
#
# =============================================================================
# QUICK START GUIDE
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate a secret key:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 3. Fill in required values in .env
#
# 4. Run migrations:
#    python manage.py migrate
#
# 5. Create a superuser:
#    python manage.py createsuperuser
#
# 6. Start the development server:
#    python manage.py runserver
#
# =============================================================================
