services:
  db:
    image: postgres:latest
    restart: always
    env_file:
      - .env
    ports:
      - "${DB_PORT_NUMBER}:5432"
    volumes:
      - db:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_DB: ${DB_NAME}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 30s
      timeout: 10s
      retries: 5

  redis:
    image: redis:latest
    restart: always
    ports:
      - "${REDIS_PORT_NUMBER}:6379"
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  rabbitmq:
    image: rabbitmq:latest
    restart: always
    ports:
      - "${RABBITMQ_PORT_NUMBER}:5672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Base service - this builds the main image ONCE
  # app_base:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     args:
  #       - SECRET_KEY=${SECRET_KEY}
  #       - DEBUG=${DEBUG}
  #       - DB_NAME=${DB_NAME}
  #       - DB_USER=${DB_USER}
  #       - DB_PASSWORD=${DB_PASSWORD}
  #       - DB_HOST=db
  #       - DB_PORT_NUMBER=${DB_PORT_NUMBER}
  #       - REDIS_HOST=redis
  #       - REDIS_PORT_NUMBER=${REDIS_PORT_NUMBER}
  #       - REDIS_PASSWORD=${REDIS_PASSWORD}
  #       - RABBITMQ_HOST=rabbitmq
  #       - RABBITMQ_PORT_NUMBER=${RABBITMQ_PORT_NUMBER}
  #       - RABBITMQ_USER=${RABBITMQ_USER}
  #       - RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
  #   image: template-backend:latest
  #   profiles: ["build-only"]  # This prevents the service from running
  #   env_file:
  #     - .env
  #   environment: &app_environment
  #     DJANGO_PORT: ${DJANGO_PORT}
  #     DB_HOST: db
  #     DB_PORT_NUMBER: ${DB_PORT_NUMBER}
  #     DB_USER: ${DB_USER}
  #     DB_PASSWORD: ${DB_PASSWORD}
  #     DB_NAME: ${DB_NAME}
  #     REDIS_HOST: redis
  #     REDIS_PORT: ${REDIS_PORT_NUMBER}
  #     REDIS_PASSWORD: ${REDIS_PASSWORD}
  #     RABBITMQ_HOST: rabbitmq
  #     RABBITMQ_PORT: ${RABBITMQ_PORT_NUMBER}
  #     RABBITMQ_USER: ${RABBITMQ_USER}
  #     RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD}
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   volumes:
  #     - app_logs:/src/logs
  #   restart: unless-stopped

  # Main API service - uses the built image
  # api_local:
  #   image: template-backend:latest
  #   container_name: api
  #   command: sh -c "./entrypoint.sh"
  #   ports:
  #     - "${DJANGO_PORT}:8000"
  #   env_file:
  #     - .env
  #   environment:
  #     <<: *app_environment
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   volumes:
  #     - app_logs:/src/logs
  #   restart: unless-stopped

  # Celery worker - uses the built image
  # celery_worker:
  #   image: template-backend:latest
  #   container_name: celery_worker
  #   command: celery -A configuration worker --loglevel=info --concurrency=4
  #   ports: []
  #   env_file:
  #     - .env
  #   environment:
  #     <<: *app_environment
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   volumes:
  #     - app_logs:/src/logs
  #   restart: unless-stopped

  # # Celery beat - uses the built image
  # celery_beat:
  #   image: template-backend:latest
  #   container_name: celery_beat
  #   command: celery -A configuration beat --loglevel=info --scheduler django_celery_beat.schedulers:DatabaseScheduler
  #   ports: []
  #   env_file:
  #     - .env
  #   environment:
  #     <<: *app_environment
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy
  #   volumes:
  #     - app_logs:/src/logs
  #   restart: unless-stopped

  # # Flower - uses the built image
  # flower:
  #   image: template-backend:latest
  #   container_name: flower
  #   command: celery -A configuration flower --port=5555 --url-prefix=flower
  #   ports:
  #     - "5555:5555"
  #   env_file:
  #     - .env
  #   environment:
  #     <<: *app_environment
  #     CELERY_BROKER_URL: amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@rabbitmq:${RABBITMQ_PORT_NUMBER}//
  #     CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:${REDIS_PORT_NUMBER}/0
  #   depends_on:
  #     - celery_worker
  #     - redis
  #     - rabbitmq
  #   volumes:
  #     - app_logs:/src/logs
  #   restart: unless-stopped

volumes:
  db:
    driver: local
  redis_data:
    driver: local
  rabbitmq_data:
    driver: local
  app_logs:
    driver: local
